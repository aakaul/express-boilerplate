/*
Note: for firebase use clase

import { getFirestore } from "firebase-admin/firestore";
import { getMessaging } from "firebase-admin/messaging";

export class FireStore {
	static db() {
		return getFirestore();
	}
	static fcm() {
		return getMessaging();
	}
	public get db(): FirebaseFirestore.Firestore {
		return FireStore.db();
	}
}
 */
